; 85_string_empty.wir
; purpose: feeds an empty string literal through const_string_ptr emission.
; verifies the string-globals path handles zero-length content correctly.
; expected runtime behavior: process exits with code 42 (call_i32 putss a blank line).

(core-module
  (core-version 1)
  (decls
    (extern puts
      (params
        (text ptr)
      ) ;; params
      (returns i32)
    ) ;; extern

    (fn main
      (params)
      (returns i32)
      (do
        (call_i32 puts
          (const_string_ptr "")
        ) ;; call_i32

        (return
          (const_i32 42)
        ) ;; return
      ) ;; do
    ) ;; fn
  ) ;; decls
) ;; core-module
