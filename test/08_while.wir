; 08_while.wir
; purpose: validates loop lowering for while with mutable locals.
; verifies repeated updates eventually produce the expected final value.
; expected runtime behavior: process exits with code 42.

(core-module
  (core-version 1)
  (decls
    (fn main
      (params)
      (returns i32)
      (do
        (let i i32
          (const_i32 0)
        ) ;; let
        (let sum i32
          (const_i32 0)
        ) ;; let
        (while
          (condition
            (lt_i32
              (local_get i)
              (const_i32 7)
            ) ;; lt_i32
          ) ;; condition
          (do
            (set sum
              (add_i32
                (local_get sum)
                (const_i32 6)
              ) ;; add_i32
            ) ;; set
            (set i
              (add_i32
                (local_get i)
                (const_i32 1)
              ) ;; add_i32
            ) ;; set
          ) ;; do
        ) ;; while
        (return
          (local_get sum)
        ) ;; return
      ) ;; do
    ) ;; fn
  ) ;; decls
) ;; core-module
