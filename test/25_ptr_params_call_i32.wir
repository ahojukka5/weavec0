; 25_ptr_params_call_i32.wir
; purpose: validates passing pointer parameters into i32-returning functions.
; verifies ptr argument plumbing and dereference-related call flow.
; expected runtime behavior: process exits with code 42.

(core-module
  (core-version 2)
  (decls
    (fn same_ptr
      (params
        (a ptr)
        (b ptr)
      ) ;; params
      (returns i32)
      (do
        (if
          (condition
            (eq_ptr
              (param_get a)
              (param_get b)
            ) ;; eq_ptr
          ) ;; condition
          (then
            (do
              (return
                (const_i32 42)
              ) ;; return
            ) ;; do
          ) ;; then
          (else
            (do
              (return
                (const_i32 0)
              ) ;; return
            ) ;; do
          ) ;; else
        ) ;; if
      ) ;; do
    ) ;; fn
    (fn main
      (params)
      (returns i32)
      (do
        (return
          (call_i32 same_ptr
            (const_null)
            (const_null)
          ) ;; call_i32
        ) ;; return
      ) ;; do
    ) ;; fn
  ) ;; decls
) ;; core-module
