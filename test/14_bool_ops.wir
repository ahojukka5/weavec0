; 14_bool_ops.wir
; purpose: exercises core boolean operators and_bool/or_bool.
; verifies bool expressions lower to i1 operations and branch safely.
; expected runtime behavior: process exits with code 42.

(core-module
  (core-version 2)
  (decls
    (fn main
      (params)
      (returns i32)
      (do
        (if
          (condition
            (and_bool
              (const_bool true)
              (or_bool
                (const_bool false)
                (const_bool true)
              ) ;; or_bool
            ) ;; and_bool
          ) ;; condition
          (then
            (do
              (return
                (const_i32 42)
              ) ;; return
            ) ;; do
          ) ;; then
          (else
            (do
              (return
                (const_i32 0)
              ) ;; return
            ) ;; do
          ) ;; else
        ) ;; if
      ) ;; do
    ) ;; fn
  ) ;; decls
) ;; core-module
